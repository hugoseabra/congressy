AWS_KEY=$${AWS_KEY}
AWS_SECRET=$${AWS_SECRET}
DOMAIN=$${DOMAIN_STAGING}
FORCE_HTTPS=$${FORCE_HTTPS_STAGING}
BUCKET_LOCATION=$${BUCKET_LOCATION}
BUCKET_NAME=$${BUCKET_NAME}
CRON_SYNC_MINUTES_OUT=$${CRON_SYNC_MINUTES_OUT}
CRON_SYNC_MINUTES_IN=$${CRON_SYNC_MINUTES_IN}
DBHOST=$${DBHOST_STAGING}
DBUSER=$${DBUSER_STAGING}
DBPASS=$${DBPASS_STAGING}
DBNAME=$${DBNAME_STAGING}
DBPORT=$${DBPORT_STAGING}
