pipeline:
  build-staging:
    image: docker
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock

    commands:
      - export IMAGE_TAG=`cat ./version`
      - |
        docker build \
          -t cgsy-staging:${IMAGE_TAG} \
          -f ./conf/staging/Dockerfile .
